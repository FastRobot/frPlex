name: StaticTests

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    container:
      image: chef/chefdk:3
    steps:
    - uses: actions/checkout@v1
    - name: lint
      uses: fastrobot/chef-cookbook-static-action@v0.1.4
    - name: rspec
      run: chef exec rspec
      
